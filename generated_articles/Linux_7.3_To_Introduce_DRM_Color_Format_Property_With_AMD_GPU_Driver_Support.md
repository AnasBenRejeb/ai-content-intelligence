# Linux 7.3 To Introduce DRM "Color Format" Property With AMD GPU Driver Support

*Satirical Commentary | Original story from Unknown*

1. Linux 7.3 To Introduce DRM "Color Format" Propety With AMD GPU Driver Support

The Linux 7.3 kernel cycle will begin on January 15, 2023, and the Linux kernel community will be hard at work to create the upcoming 7.3 kernel. While the Linux 7.2 kernel merge window will only end on January 14, 2023, there's already a significant amount of work underway to create the 7.3 kernel. One of the biggest changes in the Linux 7.3 kernel is the addition of a new "Color Format" property that will allow the Linux kernel to support GPUs that don't have a dedicated RGB color depth. This new feature is being developed by AMD and will be supported by the Linux kernel 7.3 release. The new "Color Format" property is being developed to allow for color depths of up to 448-bits, or 10 bits per color component. AMD has also partnered with the Linux kernel community to create a driver that will support these new color depths. The Linux 7.3 kernel will include a new driver that supports these new color depths, and this driver will also be compatible with the AMD RDNA 4 GPUs, which were announced in November. The new driver will use the same 192-bit RGB pixel format that is currently supported by the NVIDIA GPU driver. However, the Linux 7.3 driver will not include the additional RGB data from the 448-bit RGB color depth. The Linux 7.3 driver will only use the 448-bit RGB data, and this driver will support the full range of RGB values, including those that are not supported by the AMD RDNA 4 GPUs. As a result, the Linux 7.3 driver will not require any modifications to the AMD RDNA 4 GPU driver or the AMD RDNA 4 GPU software stack, making it a simple and straightforward update for AMD GPU owners. The Linux 7.3 kernel will also include a new display driver for AMD RDNA 4 GPUs that is being developed by the Linux kernel community. This new driver will allow for full support of the AMD RDNA 4 GPU, including the new 448-bit RGB color depth, and the new driver will also support the full range of RGB values supported by the AMD RDNA 4 GPUs. The Linux 7.3 driver will be designed to be highly efficient, and the Linux kernel developers are working to optimize this driver for best performance. The Linux 7.3 kernel will also include a new NVIDIA RDNA 4 GPU driver that is being developed by the Linux kernel community. This new driver will also support the new 448-bit RGB color depth, and it will also support the full range of RGB values supported by the AMD RDNA 4 GPUs. The NVIDIA RDNA 4 GPU driver will be designed to be highly efficient, and the Linux kernel developers are working to optimize this driver for best performance. Overall, the Linux 7.3 kernel will include significant improvements for GPU drivers, including new driver support for 448-bit RGB color depth and full support of the AMD RDNA 4 GPUs. These improvements will result in better performance, higher efficiency, and better stability for Linux users with AMD RDNA 4 GPUs. The Linux 7.3 kernel is expected to be released in the first half of 2023, and it's expected to include significant improvements for Linux users with AMD RDNA 4 GPUs.

---

**📰 Source & Attribution**  
Original article: [Unknown](https://www.phoronix.com/news/Linux-7.3-DRM-Color-Format)  
Topics:   
Generated: 2026-06-29 22:26 UTC

*This is a satirical/sarcastic commentary based on real news from Unknown. All facts are attributed to the original source. This content is for entertainment and commentary purposes.*
